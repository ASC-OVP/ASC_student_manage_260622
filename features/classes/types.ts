export type { ClassGroup, ClassGroupStatus, ClassLesson, ClassMemo, StudentClass } from "@/lib/generated/prisma";
export type ClassFilters = { q: string; grade: string; subject: string; teacherId: string; status: string };
